{#
/**
 * @file
 * Default theme implementation for a view template to display a list of rows.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 *
 * @see template_preprocess_views_style_yummy_red_our_menu()
 */
#}
        <div class="tab-content aos-init aos-animate" data-aos="fade-up" data-aos-delay="200">

          <div class="tab-pane fade active show" id="{{row.term_name}}" role="tabpanel">

            <div class="tab-header text-center">
              <p>Menu</p>
              <h3>{{row.term_name}}</h3>
            </div>

            <div class="row gy-5">
             {% for row in rows %}
              <div class="col-lg-4 menu-item">
                <a href="C:\xampp\htdocs\YummyRed\web\themes\custom\yummyred\images\menu" class="glightbox"><img src="{{row.url}}" class="menu-img img-fluid" alt=""></a>
                <h4>{{row.title}}</h4>
                <p class="ingredients">
                 {{row.Body}}
                </p>
                <p class="price">
                  {{row.Price_menu}}
                </p>
              </div><!-- Menu Item -->
             {% endfor %}
            </div>
          </div><!-- End Starter Menu Content -->
        </div>